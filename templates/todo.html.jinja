<!DOCTYPE html>
<html>
<head>
    <title>My Todo application</title>
    <style>
    .complete {
        text-decoration: line-through;
        text-decoration-color: rgb(0, 0, 0);
    }
    body {
        background: rgb(2,0,36);
        background: linear-gradient(90deg, rgb(172, 4, 178) 0%, rgb(189, 0, 82) 49%, rgb(181, 3, 12) 100%);
        font-family: ;
        
    }
    .bnSubmit{
        background-color: rgb(197, 0, 16);
    }
    
    </style>
</head>
<body>
    <h1>Welcome to my todo application</h1>
    <ul>
    {% for todo in todos %}
        <li  class= "{%if todo['complete']%} complete {% endif %}">
         {{todo['description']}}
         {% if todo['complete'] %} ✅ {%endif%}
         {% if todo['complete'] == 0%}
         <form method="POST" action="/complete_todo/{{todo['id'] }}" class="d-inline float-end">
            <input type="submit" valu e="Complete" class="btn btn-sucess">
        </form>
        {%endif%}
        {%if todo['complete']%}
        <form method="POST" action="/uncomplete_todo/{{todo['id'] }}" class="d-inline float-end">
            <input type="submit" value="Uncomplete" class="btn btn-sucess">
        </form>{%endif%}
        <form method="POST" action="/delete_todo/{{todo['id'] }}" class="d-inline float-end">
            <input type="submit" value="Delete" class="btn btn-sucess">
        </form>
        </li>
        <br>
    {%endfor%}
    
    </ul>
    <form method="POST">
        <input name="new_todos">
        <input type="submit">

    </form>
    </body>
</html>